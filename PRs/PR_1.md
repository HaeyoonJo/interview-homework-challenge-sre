# PR Title: Add documentation and solutions for Challenge 1 (Basic Search)

## Task: Challenge 1 (Basic Search) - Log Parsing

### Overview
This PR documents the solutions and explanations for challenge-1 log parsing tasks. I have documented the solutions and explanations for log parsing queries in the README.md

### Description
- Count all lines with `500` HTTP code.
- Count all `GET` requests from `yoko` to `/rrhh` location and if it was successful (`200`).
- How many requests go to `/`?
- Count all lines _without_ `5XX` HTTP code.
- Replace all `503` HTTP codes by `500`, how many requests have a `500` HTTP code?

### Testing
All commands have been verified locally using the `ubuntu:latest` Docker container and tested against the `sample.log`.

### PR status
- [x] Merged
- [x] Approved

### PR public Link
https://github.com/HaeyoonJo/interview-homework-challenge-sre/pull/1